/**
 * Java API contracts and result types that mirror the language-neutral spec.
 *
 * <p>Java extractors, such as JDT, should expose extracted facts through these
 * contracts. Non-Java extractors should implement the files under {@code spec/}
 * in their own language instead of depending on this jar.
 */
package com.poseidon.javastatic.extract.extractor;
